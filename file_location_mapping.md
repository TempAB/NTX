# File Location Mapping

| Repository Path | Host Target | Notes |
|-----------------|-------------|-------|
| `components/` | `HOST:/config/esphome/components/` | Required so ESPHome can load the external components when the builder pulls `github://TempAB/NTX@main`. |
| `esphome-jk-bms/Example YAML's/` | optional | Reference configurations only; copy manually when needed. |

